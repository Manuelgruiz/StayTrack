name: Build and publish Docker images

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: gateway
            context: .
            dockerfile: ./app/gateway/Dockerfile
            image: staytrack-gateway

          - name: auth-service
            context: .
            dockerfile: ./app/services/auth-service/Dockerfile
            image: staytrack-auth-service

          - name: user-service
            context: .
            dockerfile: ./app/services/user-service/Dockerfile
            image: staytrack-user-service

          - name: catalog-service
            context: .
            dockerfile: ./app/services/catalog-service/Dockerfile
            image: staytrack-catalog-service

          - name: goals-service
            context: .
            dockerfile: ./app/services/goals-service/Dockerfile
            image: staytrack-goals-service

          - name: stats-service
            context: .
            dockerfile: ./app/services/stats-service/Dockerfile
            image: staytrack-stats-service

          - name: tracker-service
            context: .
            dockerfile: ./app/services/tracker-service/Dockerfile
            image: staytrack-tracker-service

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: manuelgruiz
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ghcr.io/manuelgruiz/${{ matrix.image }}:latest
            ghcr.io/manuelgruiz/${{ matrix.image }}:${{ github.sha }}
